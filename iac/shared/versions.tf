terraform {
  required_version = ">= 1.6.0"
  required_providers {
    google = {
      source  = "hashicorp/google"
      version = "~> 5.0"
    }
    google-beta = {
      source  = "hashicorp/google-beta"
      version = "~> 5.0"
    }
  }
}

provider "google" {
  # Configuration will be inherited from environment variables or gcloud config
}

provider "google-beta" {
  # Configuration will be inherited from environment variables or gcloud config
}
